# test-claw-bot

A small Python project scaffold created by OpenClaw.

## Folder structure

```
test-claw-bot/
├─ README.md            # Project overview (this file)
├─ .gitignore           # Git ignore rules (venv, pyc, etc.)
└─ src/
   └─ test_claw_bot/
      ├─ __init__.py    # Marks this directory as a Python package
      └─ __main__.py    # Entry-point (run with: python -m test_claw_bot)
```

## Run

From the repo root:

```bash
python -m test_claw_bot
```
